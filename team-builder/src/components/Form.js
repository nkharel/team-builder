import React, {useState} from 'react';


function Form(props) {

    
//Declare a state for our form
    //{name: "", email: "", role: ""}    

// onChangeHandler function that updates our state in form.js

// onSubmitHandler function that calls our app addUserHandler function
// to update our app.js state

    
  return (
    <Form>
    {/* Look into events for form "onSubmit" */}
    {/* Input for name, email, role */}
    {/* <input type="text" name="name" value={mystate.name} onChange={onChangerHandler} */}
    {/* button type="submit" */}
    </Form>
  );
}

export default Form;